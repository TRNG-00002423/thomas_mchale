package com.rev.logging;

// loggers: the object that is responsible for logging the event
// appenders: the destination where your log will go (default is console)
// encoders: the object responsible for converting a event into a a log

// there is log levels as well: trace, debug, info, warn, error

// one of the most popular frameworks for logging is logBack

// slf4j (simple logging fasad for java): provides the api's for logging
// logback is the framwork that implements the api's
public class LoggingDemo {
    
}
